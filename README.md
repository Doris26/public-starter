# public-starter

Public repo with shareable Claude Code skills.

## Skills

- [`update-config`](update-config/SKILL.md) — configure the Claude Code harness via `settings.json` (permissions, hooks, env vars, MCP, plugins). User-invocable, `allowed-tools: Read`.
